//
//python -m http.server each time you start VSCODE on terminal
//url: localhost:8000
//

let config = {
    type: Phaser.CANVAS,
    width: 640,
    height: 480,
}

let game = new Phaser.Game(config);